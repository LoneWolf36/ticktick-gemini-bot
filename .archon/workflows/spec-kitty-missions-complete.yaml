name: spec-kitty-missions-complete-retired
description: |
  Retired workflow.

  This file previously hard-coded multi-mission execution and duplicated Spec
  Kitty lifecycle behavior. Do not use it for execution. Use
  spec-kitty-quality-gate.yaml plus Spec Kitty v3.1.4 lifecycle commands.

provider: codex
model: gpt-5.4-mini

nodes:
  - id: retired
    context: fresh
    prompt: |
      This workflow is retired and intentionally does not execute missions.
      Use `.archon/workflows/spec-kitty-quality-gate.yaml`.
